name: Run linters

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  linters:
    uses: fizyk/actions-reuse/.github/workflows/shared-linters-python.yml@v4.0.0
    with:
      mypy: true
      black: true
      ruff: true
